require("lsp_signature").setup({
    bind = true,
    handler_opts = {
        border = "rounded"
    },
    hint_enable = false,
    hint_prefix = "",
    hi_parameter = "LspSignatureActiveParameter",
})
